# Contributing

1. Open an issue describing the behavior change.
2. Keep live execution disabled during development.
3. Add or update tests for policy and execution changes.
4. Run `pnpm test`, `pnpm typecheck`, and `pnpm build`.
5. Never commit API keys, wallet material or real approval payloads.

Pull requests should explain the failure behavior as carefully as the happy path.
